variables
number
divisor
remaindor
initialize
number = 1\divisor =2
process
If (number < 2)
	print “its not prime!”
	number += 1
	process again
If (number = 2)remainder = number % divisor
If (remainder = 0 )
	It is not prime number, number +=1, divisor = 2, process again 
Else 
	divisor +=1
	if ( number = divisor)
		print “number is prime”, number +=1, divisor = 2, 	process again
	else
		process again

	print “number is prime”
	number += 1
If (number > 2)
	run the process in next page
for number reaches to 100
